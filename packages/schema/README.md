# @blockwright/schema

Part of [Blockwright](https://github.com/wpcoder110/blockwright), the visual page builder for Payload CMS. Install the `blockwright` package instead of depending on this one directly.
